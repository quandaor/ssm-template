<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.ssm.mapper.User1Dao">

    <insert id="insert" parameterType="com.ssm.mapper.User1Dao">
        insert into
        user1 (id,name)
        values(
        #{id},
        #{name}
        )
    </insert>

</mapper>